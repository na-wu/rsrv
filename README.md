usage:


```
mv settings.example.py settings.py

// set 'user_auth_token' to be your resy token (found via browser network)
// make changes to the 'args' variable

python3 entrypoint.py (uncomment code for parallel processing)
```